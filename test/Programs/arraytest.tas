program Test {
  func Main() {
    string[2][3] strings;

    // Array without initialisation
    int[] blah;

    // Array with initialisation
    int[3][4] numbers;

    // Assignment of nested array
    blah := numbers[0];

    numbers[0][1] := 40;
    numbers[1][2] := 50;

    int x;
    x := numbers[1][2];

    const int index := 2;

    strings[1][index] := "Hello";

    write x;
    write strings[1][index];
    write blah[1];
    write numbers[1][index] + 3;
  }
}

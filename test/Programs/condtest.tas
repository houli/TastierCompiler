program Test {
  string x;
  void Main() {
    x := 2 > 3 ? "wowee" : "zip";
    write x;
    x := 2 = 3 ? "true?" : "false?";
    write x;
    x := 2 < 3 ? "this one" : "not this";
    write x;
  }
}
